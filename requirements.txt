# FPV飞行数据分析系统 - 依赖项要求

# 核心数据处理库
numpy>=1.19.0,<2.0.0
pandas>=1.3.0,<2.0.0

# 数据可视化库
matplotlib>=3.4.0,<4.0.0

# 可选但推荐的库
scipy>=1.7.0  # 科学计算，用于高级统计分析
seaborn>=0.11.0  # 统计图表美化
jupyter>=1.0.0  # 交互式分析环境（开发用）

# 开发工具
pytest>=6.0.0  # 单元测试框架
black>=21.0.0  # 代码格式化
flake8>=3.9.0  # 代码风格检查
mypy>=0.910  # 类型检查

# 文档生成
sphinx>=4.0.0  # 文档生成器
mkdocs>=1.2.0  # 现代文档工具

# 打包和分发
setuptools>=57.0.0
wheel>=0.36.0
twine>=3.4.0

# 平台特定依赖（自动安装）
# Windows: pywin32 (如果需要)
# macOS: 通常不需要额外依赖
# Linux: 可能需要libpng-dev等系统库

# 版本兼容性说明
"""
Python版本要求: 3.7+
NumPy: 1.19.0以上，避免2.x版本的不兼容问题
Pandas: 1.3.0以上，确保数据处理的稳定性
Matplotlib: 3.4.0以上，支持最新的图表功能
"""

# 安装建议
"""
推荐使用虚拟环境安装：
python -m venv fpv_env
source fpv_env/bin/activate  # macOS/Linux
# 或
fpv_env\\Scripts\\activate    # Windows

pip install -r requirements.txt
"""

# 开发环境额外依赖
dev-requirements.txt:
pytest>=6.0.0
black>=21.0.0
flake8>=3.9.0
mypy>=0.910
sphinx>=4.0.0
mkdocs>=1.2.0